export const DEFAULT_ERROR_MESSAGE: string = 'Something went wrong';

const DEFAULT_API_URL = `https://company.com/project/orchestrator/v2`;

export const API_CONFIG = {
  DEFAULT_API_URL,

  DEFAULT_HEADERS: {
    'Content-Type': 'application/json',
  },
};
